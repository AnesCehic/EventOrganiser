import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  checkBoxInput: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 40,
    width: '80%',
    margin: 5,
    marginTop: 10,
    marginBottom: 10,
  },
});

export default styles;
