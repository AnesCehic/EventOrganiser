import React from 'react';

import {Preferences} from '@containers';

const PreferencesScreen = ({navigation}) => {
  return <Preferences navigation={navigation} />;
};

export default PreferencesScreen;
