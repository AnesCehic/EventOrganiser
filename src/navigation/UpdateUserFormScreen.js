import React from 'react';
import {UpdateUserForm} from '@containers';

const UpdateUserFormScreen = ({navigation, route}) => {
  return <UpdateUserForm navigation={navigation} />;
};

export default UpdateUserFormScreen;
